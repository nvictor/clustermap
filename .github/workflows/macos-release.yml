name: macOS Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    # Re-enable when macos-26 is no longer in preview
    # See: https://github.com/actions/runner-images
    # runs-on: macos-latest
    runs-on: macos-26

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Create build directory
        run: mkdir -p build

      - name: Import Developer ID Certificate
        run: |
          echo "${{ secrets.DEVELOPER_ID_P12 }}" | base64 --decode > build/DeveloperID.p12
          security create-keychain -p "" build.keychain
          security import build/DeveloperID.p12 -k build.keychain -P "${{ secrets.DEVELOPER_ID_P12_PASSWORD }}" -T /usr/bin/codesign
          security list-keychains -s build.keychain
          security unlock-keychain -p "" build.keychain
          security set-keychain-settings -t 3600 -l build.keychain

      - name: Configure keychain for codesign
        run: |
          security set-key-partition-list -S apple-tool:,apple: -k "" build.keychain

      - name: Install Provisioning Profile
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "${{ secrets.CLUSTERMAP_PROVISIONING_PROFILE }}" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/Clustermap.provisionprofile

      - name: Build and Archive App
        run: |
          set -o pipefail
          test -n "${{ secrets.SPARKLE_PUBLIC_ED_KEY }}"
          xcodebuild -project "Clustermap/Clustermap.xcodeproj" \
            -scheme "Clustermap" \
            -configuration Release \
            -archivePath "$PWD/build/Clustermap.xcarchive" \
            -derivedDataPath "$PWD/build/DerivedData" \
            -destination "platform=macOS,arch=arm64" \
            ARCHS=arm64 ONLY_ACTIVE_ARCH=YES \
            DEBUG_INFORMATION_FORMAT="dwarf" \
            ENABLE_HARDENED_RUNTIME=YES \
            CODE_SIGN_IDENTITY="Developer ID Application: Victor Noagbodji (2DZ6D8C78T)" \
            SPARKLE_PUBLIC_ED_KEY="${{ secrets.SPARKLE_PUBLIC_ED_KEY }}" \
            PROVISIONING_PROFILE_SPECIFIER="Clustermap" \
            archive

      - name: Export App
        run: |
          xcodebuild -exportArchive \
            -archivePath "$PWD/build/Clustermap.xcarchive" \
            -exportOptionsPlist "$PWD/Clustermap/Clustermap/ExportOptions.plist" \
            -exportPath "$PWD/build"

      - name: Notarize app
        run: |
          ditto -c -k --keepParent "build/Clustermap.app" "build/Clustermap.zip"
          xcrun notarytool submit "build/Clustermap.zip" \
            --apple-id "${{ secrets.APPLE_ID }}" \
            --team-id "${{ secrets.APPLE_TEAM_ID }}" \
            --password "${{ secrets.APP_SPECIFIC_PASSWORD }}" \
            --wait
          xcrun stapler staple "build/Clustermap.app"

      - name: Create DMG
        run: |
          hdiutil create -volname "Clustermap" \
            -srcfolder "build/Clustermap.app" \
            -ov -format UDZO "build/Clustermap.dmg"

      - name: Generate Sparkle appcast
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          test -n "$SPARKLE_PRIVATE_KEY"
          mkdir -p build/sparkle-updates
          cp "build/Clustermap.dmg" "build/sparkle-updates/Clustermap.dmg"
          printf '%s' "$SPARKLE_PRIVATE_KEY" > build/sparkle_ed25519.key
          chmod 600 build/sparkle_ed25519.key
          build/DerivedData/SourcePackages/artifacts/sparkle/Sparkle/bin/generate_appcast \
            --ed-key-file build/sparkle_ed25519.key \
            --download-url-prefix "https://github.com/nvictor/clustermap/releases/download/${GITHUB_REF_NAME}/" \
            build/sparkle-updates

      - name: Upload DMG and Sparkle appcast to Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/Clustermap.dmg
            build/sparkle-updates/appcast.xml
            build/sparkle-updates/*.delta
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
